{
  "name": "Single-element shell script — chained commands with mixed quotes",
  "executors": [
    {
      "image": "bash",
      "command": ["echo start && echo \"inner 'quotes'\" && echo end"]
    }
  ]
}
