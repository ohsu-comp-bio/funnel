Server:
  Database: dynamodb
  Databases:
    DynamoDB:
      AWS:
        Endpoint: http://localhost:8000
        Region: us-west-2

Worker:
  TaskReader: dynamodb
  TaskReaders:
    DynamoDB:
      AWS:
        Endpoint: http://localhost:8000
        Region: us-west-2
    
  ActiveEventWriters: ["dynamodb", "log"]
  EventWriter:
    DynamoDB:
      AWS:
        Endpoint: http://localhost:8000
        Region: us-west-2
