{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "funnel.fullname" . }}-rolebinding
subjects:
  - kind: ServiceAccount
    name: {{ include "funnel.serviceAccountName" . }}
roleRef:
  kind: Role
  name: {{ include "funnel.fullname" . }}-role
  apiGroup: rbac.authorization.k8s.io
{{- end }}
